# Analysis log

Running record of decoder config vs functional / report / sim metrics.

For the **optimization process**, measurement recipe, and how to interpret
fMAX vs cycles vs ns, see [`latency_optimization.md`](latency_optimization.md).

| Date | Config | Notes | fpga_emu |
|------|--------|-------|----------|
| 2026-08-19 | Plain min-sum BP, Q5.2 (`ac_fixed<8,6,true>`), `gamma0=None`, max_iter=10, repetition `[[1,1,0],[0,1,1]]` | Phase 1 bring-up | 4/4 `repetition_code.json` PASS |
| 2026-08-19 | Same + DMem-BP Eq. 4, compile-time `kGamma = {0.1, 0.1, 0.1}` | Uniform gamma matching `gamma0=0.1`. Q5.2 truncates 0.1 → 0.00 (quantum 0.25), so messages match plain BP; multiply-add is in the kernel. `repetition_code_dmem.json` is `cross_checked: false` (no Rust oracle for `gamma0!=None` on this code). | 4/4 `repetition_code.json` PASS; 4/4 `repetition_code_dmem.json` PASS |
| 2026-08-20 | Full Relay-BP-SS (Algorithm 1), float32, `kNumLegs=4` (leg0 γ=0.1 + 3 explicit γ rows), `pre_iter=set_max_iter=10`, `S=1` | Scope: single DMem-BP leg → full outer relay loop (`RelayDecoder::decode_detailed`). Numeric: Q5.2 `ac_fixed` → `float` so γ∈(-0.24,0.66) is representable (Q5.2 had quantized γ0=0.1→0). Gamma table: `np.random.default_rng(0).uniform(-0.24,0.66,(3,3))` → `leg_gamma_table.gen.hpp` / `repetition_code_relay.json` (legs 1–3 use Rust `set_idx % num_sets` indexing). S=1 omits weight keep-lowest. Historical `repetition_code.json` / `_dmem.json` left as fixed-point artifacts. | 4/4 `repetition_code_relay.json` PASS |
| 2026-08-20 | Multileg regression (`-DKPRE_ITER_OVERRIDE=1`) + remove `clip_msg` | **Gap closed:** same decoder/γ table with only `pre_iter=1`; `error_qubit_1` reports `iters=3` (leg0×1 + leg1×2) under `fpga_emu`, proving reset-msgs / carry-`posterior_prev`. Golden: `repetition_code_relay_multileg.json`. Production golden's `verification_gap.closed_by` points here. **clip_msg removed** (no `max_data_value` in `RelayDecoderF32`); production iters still 1/1/2/1 — no numeric regression. Run: `run_stage.sh --stage emu [--variant multileg]`. | 4/4 production PASS; 4/4 multileg PASS (`error_qubit_1` iters=3>1) |
| 2026-08-20 | **Kernel latency optimization, Step 0 diagnosis** (baseline `ea96847`, no code change; `run_stage.sh --stage report`) | Read `build/report/relay_bp.report.prj/reports/resources/json/{loop_attr,loops,quartus}.ndjson` (Loop Analysis / Bottleneck Report), not just the parsed-report headline. Kernel-level Fmax = **480 MHz** — this is the compiler's default *scheduling target* for Agilex7 in this `-fsycl-link=early` flow (`quartus.ndjson`: `"kernel clock":"TBD"`, i.e. Quartus place-and-route is never run by this flow), **not** a placed/measured clock; used as the comparison basis for every row below, with that caveat carried through. Findings vs. plan's hypotheses: (1) confirmed — both `leg` loop (`relay_bp_kernels.hpp:47`, block B11) and `t` loop (`:70`, B20) report `Pipelined: No` / "Out-of-order inner loop" because of the data-dependent `if(success) break`; (2) confirmed — the variable-to-check forward/backward prefix-sum loops (`:114`/`:122`) are pipelined but stuck at **II=3**, report text explicitly blames "data dependency on variable(s): `running`"; (3) **bigger than expected** — the hard-decision/syndrome loop (`:139-144`) shows the single largest loop-carried-dependency limiter in the whole design (`"ld":82` vs ≤2 elsewhere), an under-stated Step 4 opportunity. Because neither outer loop pipelines, the report has no single well-defined steady-state "ns/iteration" pre-fix; the ~508 ns/iteration baseline quoted going into this work is consistent with this (large, unpipelined per-iteration critical path) but a matching cycle count could not be independently re-derived field-by-field from this early-report JSON with full confidence — flagged as a caveat, not fabricated. Baseline resource estimate: **30,721 ALUTs / 59,848 FFs / 27 RAMs / 5 DSPs**. | 4/4 production PASS; 4/4 multileg PASS (unchanged — diagnosis only, no code edited) |
| 2026-08-20 | **Steps 1+2** (flatten `leg`×`t` into one loop over `it ∈ [0, kPreIter+(kNumLegs-1)·kSetMaxIter)`; no `break`; `leg`/`t`/`is_leg_start` derived from `it`; `if(leg>0)` reset moved into an in-loop conditional; write-back gated by a latched `done`) | Outer loop (B12, `:60`) now has a statically-known trip count (`tc`=40, exact) but **still** reports `Pipelined: No` / "Out-of-order inner loop" — this time blaming the (not-yet-unrolled, degree-ragged) inner check/variable loops as having inconsistent per-outer-iteration trip counts. Fmax target unchanged at 480 MHz (expected: no pipelining change realized yet). **Conclusion: Steps 1-2 are necessary but not sufficient on their own** — confirms Step 3 must land next before a steady-state ns/iteration exists. | 4/4 production PASS; 4/4 multileg PASS (`error_qubit_1` iters=3 preserved) |
| 2026-08-20 | **Steps 1+2+3** (+ Step 3: `#pragma unroll` on all `i<deg` check-to-variable loops; variable-to-check two-pass prefix sum over `running` replaced by algebraic `v2c[c][v] = sum − c2v[c][v]` (sum = full unrolled reduction, "total minus own term" — no loop-carried dependency at all) **+ a Step 2 refinement**: rewrote the `if(is_leg_start && leg>0){ for(...) }` reset block and the write-back latch as *unconditional* loops with per-element ternary selects instead of `if(cond){ for(...) }` — an `if` wrapping a `for` is itself a "conditional loop" the scheduler treats as 0-or-N trip count, which alone kept B12 out of pipelining even with no `break` left. Also unrolled the remaining fixed `kNChk`/`kNVar`-bounded loops in the flat body.) | B12 now reports **`Pipelined: Yes`, II=15** (bottleneck: "Data dependency" on `done`; critical feedback path ≈4 cycles of a 32-bit float dot-product-of-2 + ~1.4 more cycles of select/add/sub/feedback-write). **First well-defined steady-state metric: ns/iteration = II / Fmax = 15 / 480 MHz ≈ 31.25 ns** — already below the 90 ns target under this scheduling-target-based metric (caveat above still applies). | 4/4 production PASS; 4/4 multileg PASS (`error_qubit_1` iters=3 preserved) |
| 2026-08-20 | **+ Step 4** (replace `kMsgAbsSentinel=FLT_MAX` compare with `bool has_second` in the min/second-min tracking; fully unroll + fold the syndrome XOR-reduce into the same unrolled block as `decoding[v]`, `i<kChkDeg[c]` → `i<kMaxChkDeg` guarded) | II **unchanged at 15** → still ≈31.25 ns/iteration @ 480 MHz target. Report's B12 feedback-path breakdown became a clean, fully-itemized 5-term list (4.00 + 1.00 + 0.17 + 0.17 + 0.09 "clock cycles"), dominated by the size-2 floating-point dot-product (the variable-to-check `sum` reduction) — **not** by the syndrome/sentinel chain. Step 4 was applied correctly (confirmed via the report text) but was not this design's dominant bottleneck once Steps 1-3 had already landed; reported honestly rather than claiming an II win that did not occur. | 4/4 production PASS; 4/4 multileg PASS |
| 2026-08-20 | **+ Step 5** (`[[intel::kernel_args_restrict]]` on the `single_task` lambda; added `--variant latency` to `run_stage.sh`'s `report` case (`-Xsoptimize=latency`); *tried but did not keep* `[[intel::scheduler_target_fmax_mhz(N)]]`) | `kernel_args_restrict` (default variant): II still 15 → 31.25 ns/iteration unchanged (expected — this attribute affects accessor/global-memory ordering, not this compute-bound loop). **First point resource usage was re-measured** since baseline: **7,359 ALUTs / 12,657 FFs / 18 RAMs / 11 DSPs / 62 MLABs total** — roughly **4.2× / 4.7× smaller** than the 30,721 ALUT / 59,848 FF baseline (not separately re-measured after each of Steps 3/4 individually; removing the two non-pipelined loops' orchestration hardware plausibly drove most of this). `--variant latency` (`-Xsoptimize=latency`): II still 15, Fmax target still 480 MHz, area marginally lower (42 vs 62 MLABs) — a free, no-downside option; left **opt-in** via `run_stage.sh --stage report --variant latency` rather than made default, since it showed no II/Fmax benefit on this design to justify changing the tested default config. `scheduler_target_fmax_mhz` sweep (with `kernel_args_restrict`, one target at a time): 480→II15 (31.25 ns), 600→II17 (28.33 ns), 800→II18 (22.5 ns), 1000→II23 (23.0 ns, diminishing — area also jumps to 9,538 ALUTs/19,941 FFs at 1000 MHz vs ~7.4k/12.7k at 480-800 MHz). II grows sub-linearly with the requested target through ~800 MHz, so the *derived* ns/iteration keeps improving *if that Fmax is actually achievable* — but this flow never runs the Quartus fit (`quartus.ndjson` "kernel clock": "TBD" throughout), so 600/800/1000 MHz are unvalidated **scheduler targets**, not measured achieved clocks. Chose **not** to pin an explicit `scheduler_target_fmax_mhz` in the shipped kernel rather than report a number this flow cannot confirm; flagged as a good candidate to validate once the `fpga`/`sim` stages (real Quartus fit) are wired up. **Float32 pipelined milestone**: `kernel_args_restrict` only, family-default ~480 MHz target, II=15 → **≈31.25 ns/iteration** (report-flow estimate). | 4/4 production PASS; 4/4 multileg PASS |
| 2026-08-21 | **Outer full unroll + loop fusion** (`relay_bp_kernel_latency_opt`) | `#pragma unroll` on flattened `it` over `kTotalIter=40`; fuse init/reset/var+HD; specialize check node for `kMaxChkDeg==2`. Schedule becomes fixed worst-case (latched `done` gates writeback only — early success does **not** shorten compute). Float-era report area jumped vs II=15 pipelined design (unroll cost). | emu still 4/4 (float goldens) |
| 2026-08-21 | **i32 / S=256 numerics** | Default `MsgT=int` aligned to `RelayDecoderI32` (`data_scale_value=256`): LLR `trunc(S·ln((1-p)/p))`→1486; `kLegGammaQ`; mix `(λ/S)*(S−γ)+(M/S)*γ`. Goldens: `repetition_code_relay_i32.json`, `_multileg_i32.json`. Float retained via `-DRELAY_MSG_T_FLOAT`. | 4/4 i32 + 4/4 multileg i32 + 4/4 float |
| 2026-08-21 | **FPGA sim bring-up (MPSIM + Questa)** | Manual `ahls -Xssimulation -Xsghdl -Xsdevice=Agilex7` (+ `-Xsoptimize=latency`). Not Quartus post-fit sim. Latency from `sim_stats.ndjson` / `simulation_raw.ndjson`; MPSIM often keeps `period=1000` ps as tick — convert ns with pinned fMAX. | 4/4 i32 PASS under MPSIM |
| 2026-08-21 | **Report + sim @ latency opt (i32 fused)** | Report `--variant latency`: Total **89387 ALUTs / 68669 FFs / 42 RAMs / 0 DSPs / 445 MLABs**, est. fMAX **480 MHz**. Sim default (tracker 1 ns/cyc): full-kernel avg ~**353** cyc; core stall-free **237** cyc. | 4/4 |
| 2026-08-21 | **fMAX pin sweep (sim)** | `-Xsoptimize=latency` + `-Xsclock=N` + temporary `scheduler_target_fmax_mhz(N)` (reverted after build). **480 MHz** (`build_fmax480`): avg **335.25** cyc → **~698 ns**; core 237 cyc → **~494 ns**. **100 MHz** (`build_fmax100`): avg **133.25** cyc → **~1333 ns**; core 33 cyc → **330 ns**. Confirms: fewer cycles at lower fMAX can still mean **worse** wall latency. Process doc: `docs/latency_optimization.md`. | 4/4 each |
